import { Link } from '@tanstack/react-router'
import { CalendarDays, Clock3, MapPin } from 'lucide-react'
import type { EventItem } from '@/types'
import { Button } from '@/components/ui/button'
export function EventCard({event}:{event:EventItem}){return <article className="group flex h-full flex-col overflow-hidden rounded-[20px] border border-border bg-card shadow-card transition-all duration-300 hover:-translate-y-1 hover:shadow-card-hover"><Link to="/events/$eventId" params={{eventId:event.id}} className="block overflow-hidden"><img src={event.image} alt={`${event.title}, a sample ${event.category.toLowerCase()} performance in Pune`} width={1280} height={800} loading="lazy" className="aspect-[16/10] w-full object-cover transition-transform duration-500 group-hover:scale-[1.04]"/></Link><div className="flex flex-1 flex-col p-5"><span className="w-fit rounded-full bg-accent px-2.5 py-1 text-xs font-bold uppercase text-primary">{event.category}</span><h3 className="mt-3 line-clamp-1 font-display text-xl font-bold">{event.title}</h3><div className="mt-4 grid gap-2 text-sm text-muted-foreground"><span className="flex items-center gap-2"><CalendarDays className="size-4 shrink-0 text-primary"/>{event.date}</span><span className="flex items-center gap-2"><Clock3 className="size-4 shrink-0 text-primary"/>{event.time}</span><span className="flex min-w-0 items-center gap-2"><MapPin className="size-4 shrink-0 text-primary"/><span className="truncate">{event.venue}, {event.city}</span></span></div><div className="mt-auto flex items-center justify-between gap-3 pt-5"><div><span className="text-xs text-muted-foreground">From</span><p className="text-lg font-extrabold">₹{event.price}</p></div><Button asChild><Link to="/events/$eventId" params={{eventId:event.id}}>Book Tickets</Link></Button></div></div></article>}
export function EventGrid({items}:{items:EventItem[]}){return <div className="grid gap-6 sm:grid-cols-2 xl:grid-cols-3">{items.map(event=><EventCard key={event.id} event={event}/>)}</div>}
