import { Search } from 'lucide-react'
import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
export function SearchBar({value,onChange}:{value:string,onChange:(v:string)=>void}){return <label className="relative block"><span className="sr-only">Search events</span><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground"/><Input value={value} onChange={e=>onChange(e.target.value)} placeholder="Search theatre, events or performers..." className="h-12 rounded-xl bg-background pl-12"/></label>}
export function StatusBadge({status}:{status:string}){const good=['Approved','Published','Confirmed','Upcoming'].includes(status);const bad=['Rejected','Failed'].includes(status);return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-bold ${good?'bg-success-soft text-success':bad?'bg-error-soft text-destructive':'bg-accent text-primary'}`}>{status}</span>}
export function QuantitySelector({value,onChange}:{value:number,onChange:(v:number)=>void}){return <div className="flex h-10 items-center rounded-lg border border-border"><Button variant="ghost" size="icon" aria-label="Decrease quantity" onClick={()=>onChange(Math.max(0,value-1))}>−</Button><span className="w-8 text-center font-bold" aria-live="polite">{value}</span><Button variant="ghost" size="icon" aria-label="Increase quantity" onClick={()=>onChange(Math.min(10,value+1))}>+</Button></div>}
export function PageIntro({eyebrow,title,copy}:{eyebrow?:string,title:string,copy:string}){return <div className="max-w-2xl">{eyebrow&&<p className="mb-2 text-sm font-bold uppercase text-primary">{eyebrow}</p>}<h1 className="font-display text-4xl font-extrabold sm:text-5xl">{title}</h1><p className="mt-4 text-lg leading-8 text-muted-foreground">{copy}</p></div>}
