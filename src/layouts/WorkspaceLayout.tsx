import { Link, useRouterState } from '@tanstack/react-router'
import { ArrowLeft, CalendarDays, ClipboardCheck, LayoutDashboard, PlusCircle, ReceiptText } from 'lucide-react'
import { Brand } from '@/components/app/Brand'
import { Button } from '@/components/ui/button'
const organizer=[['/organizer','Overview',LayoutDashboard],['/organizer/events','My Events',CalendarDays],['/organizer/events/create','Create Event',PlusCircle],['/organizer/orders','Orders',ReceiptText]] as const
const admin=[['/admin','Overview',LayoutDashboard],['/admin/events','Pending Events',ClipboardCheck]] as const
export function WorkspaceLayout({kind,children}:{kind:'organizer'|'admin';children:React.ReactNode}){const path=useRouterState({select:s=>s.location.pathname});const links=kind==='organizer'?organizer:admin;return <div className="min-h-screen bg-soft"><header className="border-b border-border bg-background"><div className="mx-auto grid h-16 max-w-7xl grid-cols-[minmax(0,1fr)_auto] items-center gap-4 px-4 sm:px-6"><div className="flex min-w-0 items-center gap-4"><Brand/><span className="hidden rounded-full bg-accent px-3 py-1 text-xs font-bold uppercase text-primary sm:inline">{kind} demo</span></div><Button variant="ghost" asChild><Link to="/"><ArrowLeft/> Customer site</Link></Button></div></header><div className="mx-auto grid max-w-7xl md:grid-cols-[220px_1fr]"><aside className="border-b border-border bg-background p-3 md:min-h-[calc(100vh-64px)] md:border-b-0 md:border-r md:p-5"><nav className="flex gap-2 overflow-x-auto md:grid">{links.map(([to,label,Icon])=><Link key={to} to={to} className={`flex shrink-0 items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-semibold ${path===to?'bg-accent text-primary':'text-muted-foreground hover:bg-soft hover:text-foreground'}`}><Icon className="size-4"/>{label}</Link>)}</nav></aside><main className="min-w-0 p-4 sm:p-6 lg:p-10">{children}</main></div></div>}
